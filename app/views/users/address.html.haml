= render 'toppage/header'
= render "toppage/button"
= render "user_pankz"


.user-main
  = render "side"
  .address__content
    .address__content__head
      %h2.address__content__head__text 発送元・お届け先住所変更
    .address__content__form
      = form_for @user, url: address_update_user_path, method: :patch do |f|
        .form-group
          %label お名前
          %span.form-require 必須
          %br
          = f.text_field :family_name, placeholder:"例) 山田",class:"input-default"
          %br
          = f.text_field :first_name, placeholder:"例) 彩",class:"input-default"
        .form-group
          %label お名前カナ
          %span.form-require 必須
          = f.text_field :family_kana, placeholder:"例) ヤマダ",class:"input-default"
          = f.text_field :first_kana, placeholder:"例) アヤ",class:"input-default"
          = f.fields_for @address do |a|
            .form-group
            %label 郵便番号
            %span.form-require 必須
            %br
            = a.text_field :post, placeholder:"例) 123-4567",class:"input-default"
            .form-group
            %label 都道府県
            %span.form-require 必須
            %br
            .select-prefecture
              %i.icon-arrow-bottom
                = a.collection_select :prefecture, Area.all, :name, :name, { prompt: '---' }
            .form-group
            %label 市区町村
            %span.form-require 必須
            %br
            = a.text_field :city, placeholder:"例) 横浜市緑区",class:"input-default"
            .form-group
            %label 番地
            %span.form-require 必須
            %br
            = a.text_field :address, placeholder:"例) 青山1−1−1",class:"input-default"
            .form-group
            %label 建物名
            %span.form-arbitrary 任意
            = a.text_field :buil, placeholder:"例) 柳ビル103",class:"input-default"
            .form-group
            %label 電話
            %span.form-arbitrary 任意
            = f.text_field :phone_number, placeholder:"例) 09012345678",class:"input-default"
        = f.submit "変更する", class: "btn-red"


= render 'toppage/footer'